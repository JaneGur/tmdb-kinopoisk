# 🤝 Contributing to TMDB Kinopoisk App

Спасибо за интерес к проекту! Мы приветствуем любой вклад в развитие приложения.

---

## 📋 Code of Conduct

Участвуя в этом проекте, вы соглашаетесь соблюдать наш Code of Conduct:
- Будьте уважительны к другим участникам
- Конструктивно критикуйте код, а не людей
- Примите, что разные мнения существуют
- Помогайте новичкам

---

## 🚀 Как начать

### 1. Fork репозитория
```bash
# Кликните кнопку "Fork" на GitHub
```

### 2. Клонируйте свой fork
```bash
git clone https://github.com/YOUR_USERNAME/tmdb-kinopoisk-app.git
cd tmdb-kinopoisk-app
```

### 3. Добавьте upstream remote
```bash
git remote add upstream https://github.com/ORIGINAL_OWNER/tmdb-kinopoisk-app.git
```

### 4. Установите зависимости
```bash
npm install
```

### 5. Создайте .env файл
```bash
cp .env.example .env
# Добавьте свои TMDB API ключи
```

### 6. Запустите dev-сервер
```bash
npm run dev
```

---

## 🔄 Процесс разработки

### 1. Синхронизируйте с upstream
```bash
git checkout main
git pull upstream main
git push origin main
```

### 2. Создайте feature ветку
```bash
# Используйте префиксы:
# feature/ - новая функциональность
# fix/     - исправление бага
# docs/    - документация
# refactor/ - рефакторинг
# style/   - стили/форматирование
# test/    - тесты

git checkout -b feature/add-user-reviews
```

### 3. Разработка
- Пишите чистый, читаемый код
- Следуйте существующему стилю кода
- Добавляйте комментарии к сложной логике
- Используйте TypeScript типы

### 4. Commit изменений
```bash
# Используйте conventional commits:
# feat: новая функциональность
# fix: исправление бага
# docs: документация
# style: форматирование
# refactor: рефакторинг
# test: тесты
# chore: обновление зависимостей и т.д.

git add .
git commit -m "feat: add user reviews functionality"
```

### 5. Push в свой fork
```bash
git push origin feature/add-user-reviews
```

### 6. Создайте Pull Request
- Откройте PR на GitHub
- Заполните шаблон PR
- Дождитесь review

---

## 📝 Стандарты кода

### TypeScript
```typescript
// ✅ Хорошо
interface Movie {
  id: number
  title: string
  posterPath: string | null
}

// ❌ Плохо
interface Movie {
  id: any
  title: any
  posterPath: any
}
```

### Именование
```typescript
// Components - PascalCase
export function MovieCard() {}

// Hooks - camelCase с префиксом use
export function useMovies() {}

// Constants - UPPER_SNAKE_CASE
export const API_BASE_URL = 'https://api.themoviedb.org/3'

// Functions - camelCase
export function formatDate(date: string) {}
```

### Структура компонентов
```typescript
// 1. Imports
import { useState } from 'react'
import styles from './Component.module.css'

// 2. Types/Interfaces
interface ComponentProps {
  title: string
}

// 3. Component
export function Component({ title }: ComponentProps) {
  // 3.1. Hooks
  const [state, setState] = useState()
  
  // 3.2. Handlers
  const handleClick = () => {}
  
  // 3.3. Render
  return <div>{title}</div>
}
```

### CSS Modules
```css
/* Используйте осмысленные имена классов */
.movieCard {}
.movieCard__title {}
.movieCard__rating {}
.movieCard--featured {}

/* Избегайте глубокой вложенности */
/* ✅ Хорошо */
.card {}
.cardTitle {}

/* ❌ Плохо */
.card .inner .title {}
```

---

## 🏗 Архитектура (FSD)

### Слои (сверху вниз)
```
app/        - инициализация приложения
pages/      - страницы приложения
widgets/    - составные блоки
features/   - бизнес-логика
entities/   - бизнес-сущности
shared/     - переиспользуемый код
```

### Сегменты
```
ui/         - UI компоненты, стили
api/        - API взаимодействие
model/      - типы, схемы, store
lib/        - утилиты
config/     - конфигурация
```

### Пример
```typescript
// ✅ Правильно
import { MovieCard } from '@entities/movie'
import { useGetMoviesQuery } from '@features/movies'
import { Button } from '@shared/ui/button'

// ❌ Неправильно
import { MovieCard } from '../../entities/movie/ui/MovieCard'
```

---

## 🧪 Тестирование

### Unit тесты (будущее)
```typescript
import { render, screen } from '@testing-library/react'
import { MovieCard } from './MovieCard'

describe('MovieCard', () => {
  it('renders movie title', () => {
    render(<MovieCard movie={mockMovie} />)
    expect(screen.getByText('Movie Title')).toBeInTheDocument()
  })
})
```

### Integration тесты (будущее)
```typescript
import { renderWithProviders } from '@shared/lib/test-utils'
import { HomePage } from './HomePage'

it('fetches and displays movies', async () => {
  renderWithProviders(<HomePage />)
  // ...
})
```

---

## 📚 Что можно улучшить

### Приоритет: Высокий
- [ ] Unit тесты для компонентов
- [ ] E2E тесты
- [ ] Accessibility (a11y) улучшения
- [ ] SEO оптимизация
- [ ] Performance оптимизации

### Приоритет: Средний
- [ ] Infinite scroll
- [ ] Virtual scrolling
- [ ] PWA поддержка
- [ ] Мультиязычность (i18n)
- [ ] Авторизация пользователей

### Приоритет: Низкий
- [ ] Социальные функции
- [ ] Рейтинг фильмов
- [ ] Списки просмотра
- [ ] Комментарии и обсуждения

---

## 🐛 Сообщение об ошибках

### Перед созданием Issue:
1. Проверьте, что issue еще не создан
2. Убедитесь, что используете последнюю версию
3. Попробуйте воспроизвести на чистой установке

### Шаблон Issue:
```markdown
## Описание бага
Краткое описание проблемы

## Шаги для воспроизведения
1. Откройте '...'
2. Кликните на '...'
3. Прокрутите до '...'
4. Увидите ошибку

## Ожидаемое поведение
Что должно произойти

## Фактическое поведение
Что происходит на самом деле

## Скриншоты
Если применимо

## Окружение
- OS: [например, Windows 11]
- Browser: [например, Chrome 120]
- Version: [например, 1.0.0]

## Дополнительный контекст
Любая другая информация
```

---

## 💡 Предложение новой функции

### Шаблон Feature Request:
```markdown
## Описание функции
Что вы хотите добавить?

## Зачем это нужно?
Какую проблему это решает?

## Предложенное решение
Как это должно работать?

## Альтернативы
Рассматривали ли вы другие варианты?

## Дополнительный контекст
Mockups, примеры, ссылки
```

---

## ✅ Pull Request Checklist

Перед отправкой PR убедитесь:
- [ ] Код следует стандартам проекта
- [ ] Все типы TypeScript корректны (нет `any`)
- [ ] Нет console.log/console.error (используйте logger)
- [ ] Код отформатирован (ESLint)
- [ ] Компоненты имеют JSDoc комментарии
- [ ] CSS Modules используются корректно
- [ ] Нет хардкода (используйте константы)
- [ ] README обновлен (если нужно)
- [ ] CHANGELOG обновлен (если нужно)

---

## 📖 Полезные ресурсы

### Документация
- [React](https://react.dev/)
- [TypeScript](https://www.typescriptlang.org/docs/)
- [Redux Toolkit](https://redux-toolkit.js.org/)
- [React Router](https://reactrouter.com/)
- [Feature-Sliced Design](https://feature-sliced.design/)
- [TMDB API](https://developer.themoviedb.org/docs)

### Инструменты
- [Vite](https://vite.dev/)
- [ESLint](https://eslint.org/)
- [Zod](https://zod.dev/)
- [React Toastify](https://fkhadra.github.io/react-toastify/)

---

## 🎯 Приоритеты для новых контрибьюторов

### Good First Issues
Хорошие задачи для начала:
- Исправление опечаток в документации
- Добавление JSDoc комментариев
- Улучшение стилей
- Исправление мелких багов
- Добавление констант вместо хардкода

### Help Wanted
Задачи, требующие помощи:
- Unit тесты
- Accessibility улучшения
- Performance оптимизации
- Мультиязычность

---

## 💬 Вопросы?

Если у вас есть вопросы:
1. Проверьте [FAQ](docs/FAQ.md)
2. Поищите в [Issues](https://github.com/yourusername/tmdb-kinopoisk-app/issues)
3. Создайте новый Issue с меткой `question`

---

## 🙏 Спасибо!

Спасибо за ваш вклад в проект! Каждый PR, Issue и комментарий ценятся.

**Happy coding! 🎬✨**
